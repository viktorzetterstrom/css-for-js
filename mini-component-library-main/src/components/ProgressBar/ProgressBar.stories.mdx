import { Meta, Story } from "@storybook/addon-docs/blocks";
import ProgressBar from "./ProgressBar";

<Meta
  title="ProgressBar"
  component={ProgressBar}
  argTypes={{
    value: {
      control: {
        type: "number",
        min: 0,
        max: 100,
      },
    },
  }}
/>

export const Template = (args) => (
  <div>
    <p style={{ marginBottom: 4, marginTop: 4 }}>Large</p>
    <ProgressBar {...args} size="large" />
    <p style={{ marginBottom: 4, marginTop: 4 }}>Medium</p>
    <ProgressBar {...args} size="medium" />
    <p style={{ marginBottom: 4, marginTop: 4 }}>Small</p>
    <ProgressBar {...args} size="small" />
  </div>
);

# ProgressBar

<Story name="Default" args={{ value: 70, size: "medium" }}>
  {Template.bind({})}
</Story>
